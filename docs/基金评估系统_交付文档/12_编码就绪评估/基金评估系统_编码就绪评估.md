# 基金评估系统 · 编码就绪度评估报告

> **评估日期**：2025-07-28
> **评估范围**：Phase 1 MVP 是否具备编码启动条件
> **触发**：Req H（推送全部更新至 GitHub + 重新评估编码就绪度）
> **依据文档版本**：详见文末附录

---

## 0. 一句话结论

**可以进入编码阶段（Phase 1 MVP）。**

设计文档（BRD / 技术规格 / 详细设计）、6 份技术方案（E1–E14 专家修订全闭环）、开发规范（Streamlit 对齐）、CLAUDE.md 编码约束基线均已就绪并推送至 GitHub（`ea80452`）。原《模块复杂度评估与排序》的「BK-1/2/3 阻塞」与「6 模块不可直接编码」判定为 TP 文档产出**之前**的基线，已被后续 TP-01~06 + 专家评审实质性闭合。

仅余 **1 项硬依赖**（LLM 供应商与 key，对应 TP-06 的 R7）与若干**工程就绪优化**需并行推进，均**不阻塞** Phase 1 启动。

---

## 1. 编码就绪度重判（R1–R7 rubric）

对照《模块复杂度评估与排序》§1.2 的判定 rubric，按 TP 闭环后的最新状态重判：

| 判定项 | 含义 | TP 闭环后 | 依据 |
|--------|------|-----------|------|
| R1 接口契约 | 输入/输出字段、类型、枚举已明确 | ✅ 满足 | 详细设计 §2.21 统一信封 + 各模块 §3.x |
| R2 数据模型 | 表/字段/DDL 已明确 | ✅ 满足 | 详细设计 §2.20 ER + DDL |
| R3 算法步骤 | 精确公式 + 计算步骤 + 数据来源 | ✅ 满足（原 ✗） | TP-01~06 给出精确公式与伪代码 |
| R4 边界与异常 | 缺失值/失败/降级/口径约定 | ✅ 满足（原 △） | E8/E9/E12 阈值联动；TP-02 兜底引擎 |
| R5 并发边界 | 并发模型/锁/缓存一致性 | ✅ 满足 | 详细设计 §2.22 性能与并发优化 |
| R6 测试验收 | 可量化 test oracle | ✅ 满足（原 △） | NL 评测集 160 条；TP 对账基准 |
| R7 外部依赖 | 外部 API/LLM 接入、兜底、配额 | △ 部分满足 | AkShare/Tushare 已定义 fallback；**LLM 具体供应商/key 待确认**（TP-06） |

> **判定结论**：R1–R6 全部满足；R7 仅 LLM 具体供应商与 key 待落地——架构（LangChain + Chroma + cheap LLM）与失败回退约定已存在，属于「确认类」而非「设计类」阻塞，不阻断算法层编码。

---

## 2. 原复杂度评估阻塞项（BK-1/2/3）闭合核对

| 阻塞项 | 原判定 | 现状 | 闭合证据 |
|--------|--------|------|----------|
| **BK-1** 五因子口径三文档冲突 | 阻塞 | ✅ 已闭合 | 专家评审 **E4** + 详细设计 §3.3.8.1 统一为 `ret/risk/perf/scale/manager`，与 BRD/技术规格一致；§2.21 枚举统一 |
| **BK-2** 各因子原始指标未定义 | 阻塞 | ✅ 已闭合 | TP-01 §3 给出指标映射表（年化收益/最大回撤/Sharpe/规模/任职回报等） |
| **BK-3** 横截面百分位在线-批算一致性 | 阻塞 | ✅ 已闭合 | TP-01 §4 明确批算产出全市场分位表缓存、在线查询复用分位表（ADR-002 唯一权威源） |

> ⚠ **重要提示**：《模块复杂度评估与排序》§3/§5 仍标注「不可直接编码」与「BK-1/2/3 阻塞」，该判定为 TP 文档产出**前**基线，现已被推翻。建议在原文档头加「状态更新」备注（见 §5 修复项 F1），避免 AI 编码工具误读为现行阻塞条件。

---

## 3. 各模块可编码结论（与 INDEX.md 一致）

| 模块 | 结论 | 依据 |
|------|------|------|
| 评估引擎 TP-01 | **可直接编码** | E1/E2/E4/E5/E7/E13 闭环 |
| NL 选基 TP-02 | 可编码 | E6 闭环；LLM 接入待 key |
| 组合诊断 TP-03 | 可编码 | E8/E9/E12 闭环 |
| 回测引擎 TP-04 | 可编码（由「不可编码」降级） | E3/E14 闭环 |
| 宏观底盘 TP-05 | 可编码（由「不可编码(中)」升级） | E10/E11/E8 闭环 |
| AI 助手 TP-06 | 可编码（R7 依赖 key） | RAG 设计就绪，LLM key 待注入 |

---

## 4. 仍需「确认」的任务（Confirms / 决策点）

| 编号 | 事项 | 说明 | 阻塞级别 |
|------|------|------|----------|
| **C1** | LLM 供应商与 Key（TP-06 R7 唯一硬依赖） | 具体模型（DeepSeek / Qwen / 通义）、API key 注入方式（.env / secret）、配额与降级（失败回退规则层）。NL 选基 SLA≥85% 依赖 LLM，提示词设计稿已就绪待接入 | 硬依赖（仅 TP-06） |
| **C2** | 数据源授权 | AkShare（主，免费）已可用；Tushare（备）需 token，确认获取与额度 | 软（已有 fallback） |
| **C3** | 环境底座形态 | PostgreSQL + Redis（ADR-004）部署方式（docker-compose 本地 / 云托管），确认后提供脚手架 | 工程就绪 |
| **C4** | 原型映射策略 | `fund_invest_prototype.html` 为静态原型，确认是否 1:1 映射为 Streamlit `pages/` 还是仅作视觉参考 | 非阻塞 |
| **C5** | PAT 安全（见 §8） | 当前复用同一 GitHub PAT，属安全决策点，建议立即轮换 | 安全 |

---

## 5. 仍需「修复」的任务（Fixes / 文档一致性补丁）

| 编号 | 事项 | 说明 | 优先级 |
|------|------|------|--------|
| **F1** | 复杂度评估文档「状态更新」备注 | 在原文档头加备注，指向 TP-01~06 + E1–E14 闭环，撤销「不可直接编码 / BK 阻塞」的现行误导，避免 AI 编码误读 | **高** |
| **F2** | 开发规范权威源去重 | `/workspace` 根目录散落 `基金评估系统_开发规范.md` 与 `11_开发规范/` 副本重复，确认权威源 = `11_开发规范/`，清理根目录散落副本 | 中 |
| **F3** | 测试残留清理 | `/workspace/SYNC_TEST_请删除我.txt` 为测试残留，应删除（不入库，但避免污染工作区） | 低 |
| **F4** | 开发规范补「AI 编码约束」小节 | 显式指向 CLAUDE.md，增强可读性（非必须） | 低 |

---

## 6. 仍需「优化」的任务（Optimizations / 工程就绪）

| 编号 | 事项 | 说明 |
|------|------|------|
| **O1** | 测试 fixture / mock | AkShare/Tushare 网络依赖需 mock（respx / vcrpy）；NL 评测集 160 条（`nl_eval_set.json` / `nl_eval_set_adv.json`）可固化为回归语料 |
| **O2** | pre-commit + CI | `.pre-commit-config.yaml`（black/ruff/mypy/pyright）与 GitHub Actions（lint+test+type）尚未落地；CLAUDE.md §8 列 run 命令但 CI 门禁未建 |
| **O3** | DDL 落地 | 详细设计 §2.20 数据模型 ER+DDL 尚未转为可执行 migration（Alembic） |
| **O4** | 接口契约锁定 | FastAPI 自动 OpenAPI 需生成版本化 schema 文档并纳入变更评审 |
| **O5** | 日志/监控底座 | 结构化日志 + trace_id 埋点（开发规范 §5）需在 api/worker 启动骨架落位 |
| **O6** | 持仓穿透「财务四维卡」字段定义 | 复杂度评估 §4 备注（非阻塞）：补来源/报告期/四维度指标，可在 P1 实现期补 |

---

## 7. 进入编码前建议动作清单（轻量，非阻塞）

建议 Phase 1 启动前（或首个 sprint 并行）完成以下 5 项：

1. **F1**：复杂度评估文档头加「状态更新」备注（防 AI 误读阻塞）——**最高优先**。
2. **C1/C2**：确认并注入 LLM key + 数据源 token（env/secret 管理）——唯一 R7 硬依赖。
3. **C3**：提供 `docker-compose.yml`（PG + Redis + api + worker + streamlit）脚手架——工程就绪。
4. **O2**：建立 `.pre-commit-config.yaml` + 最小 GHA——质量门禁。
5. **F2/F3**：清理根目录散落文件（开发规范副本、SYNC_TEST）。

> 以上均不阻断 Phase 1 启动；其中 F1 与 C1 建议在 P1-03*（评估算法）与 P1-06b（NL 解析）开工前完成。

---

## 8. 安全提示（PAT 轮换）

本次推送复用同一 GitHub PAT（`ghp_***`）。该 token 已在对话中明文出现，**存在泄露风险**。

**强烈建议**：
- GitHub 后台立即 **revoke** 该 PAT 并新建最小权限 token（仅 `repo` 写权限）；
- 新 token 通过 secret / env 注入，**勿明文留存于对话或代码**；
- 本仓库推送已完成（`ea80452`），轮换不影响已入库内容。

---

## 附录：依据文档版本

| 文档 | 版本/状态 |
|------|-----------|
| 业务需求说明书 BRD | v1.2 |
| 技术规格说明书 | v1.3 |
| 详细设计说明书 | v1.1（含 E1–E14 闭环） |
| 技术方案 TP-01~06 + 专家评审报告 | 已闭环 E1–E14 |
| 开发任务分解 | v1.1（Phase 1 里程碑 08-18 ~ 10-05） |
| 模块复杂度评估与排序 | §3/§5 判定为 TP 前基线（见 §2 / §5 F1） |
| 开发规范 | v1.0（Streamlit 对齐） |
| CLAUDE.md | 编码约束基线（绑定设计/规范/TP/E1–E14） |
| 推送状态 | 远程 HEAD = `ea80452` |

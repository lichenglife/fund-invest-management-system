# CLAUDE.md · 基金评估与模拟交易系统（FundLens）

> **本文件是 Claude Code 在本仓库编码时的强制约束与导航。每次编码前必须先读本文，并严格按"必须引用"指令查阅对应文档。违反约束须立即停止并澄清。**

---

## 0. 一句话定位

面向**个人投资者 / 学习者**的 Python 全栈基金评估与模拟交易系统（FundLens），覆盖"学→懂→筛→练→评→穿"12 个功能模块。**MVP 阶段为单用户**，不做实盘下单执行。

---

## 1. 硬约束（最高优先级）

1. **不偏离需求与技术路线**：所有实现以 BRD / 技术规格 / 详细设计 / 技术方案(TP) 为唯一权威，**禁止**自创需求、更换技术栈或重设算法口径。
2. **编码前必须读对应文档并引用**：实现任意模块前，必须阅读 §3 文档地图中该模块对应的文件与章节，并在代码注释/提交中注明引用（如 `# 见 TP-01 §3.3 / 详细设计 §3.3.8`）。**禁止凭记忆实现金融算法**。
3. **按开发计划执行**：任务唯一来源为 `基金评估系统_开发任务分解.md`(v1.1)；每个任务关联 `FR-/DC-` 需求 ID 并遵循 DoD；**禁止**擅自扩大范围或新增计划外任务，范围存疑先提问。
4. **遵守开发规范**：`基金评估系统_开发规范.md`(v1.0) 全部条款为强制基线（代码 / 目录 / 文档 / 提交 / 日志 / API / 错误 / 异常 / 发布 / 前端）。
5. **金融正确性红线**：§4 所列 E1–E14 已闭环项**禁止回退**。
6. **变更须先登记评审（需求变更管理规范）**：开发阶段任何**超出引用文档的设计 / 库表 / 接口 / 算法口径**改动，必须先在根目录 `CHANGELOG.md` 登记 CR（`CR-<YYYYMMDD>-<序号>`）并经对应级别评审（**C1 轻量 / C2 中度 / C3 重度**），**禁止自行变更**。硬约束 **R1–R8**（R1 禁止偏离设计 / R2 金融红线不可退 / R3 库变更走 Alembic 可回滚 / R4 删除最严 / R5 接口稳定 / R6 删除需求纪律 / R7 文档同步强制 / R8 可追溯回滚）违反即拦截合并 / 发布。完整流程与模板见 `13_需求变更管理规范/基金评估系统_需求变更管理规范.md`。

---

## 2. 技术栈（固定，禁止擅自更换）

| 层 | 选型 |
|----|------|
| 运行时 | **Python 3.11+** |
| 内部 API | FastAPI |
| 前端 | **Streamlit 1.35+（纯 Python）**——**不引入 TS/JS 前端工程**（与详细设计 §2.7 一致） |
| 存储 | PostgreSQL + Redis（ADR-004 多副本必需） |
| 算法 | pandas / empyrical / vectorbt / statsmodels |
| AI | LangChain + Chroma + 低价 LLM（DeepSeek / 通义千问） |
| 数据采集 | AkShare（主）+ Tushare（备），须有 fallback（§2.15） |

---

## 3. 文档地图（编码前必读，按模块引用）

> 下列文件位于（`docs/基金评估系统_交付文档/<NN>_*/`。

| 主题 | 必读文件（精确路径） |
|------|----------------------|
| 需求总纲 | `基金评估系统业务需求说明书.md`（BRD v1.2）、`需求确认记录_grill-me审查纪要.md` |
| 技术蓝图 | `基金评估系统技术规格说明书.md`（v1.3） |
| 实现依据 | `基金评估系统详细设计说明书.md`（v1.1）——按 §3.x 定位模块 |
| 高难模块算法 | `基金评估系统_技术方案_评估引擎.md`(TP-01)、`_NL选基.md`(TP-02)、`_组合诊断.md`(TP-03)、`_回测引擎.md`(TP-04)、`_宏观底盘.md`(TP-05)、`_AI助手.md`(TP-06) |
| 专家评审闭环 | `基金评估系统_技术方案_专家评审报告.md`（E1–E14） |
| 开发计划 | `基金评估系统_开发任务分解.md`（v1.1，对应 `基金评估系统_交付文档/09_开发计划/`） |
| 规范 | `基金评估系统_开发规范.md`（v1.0，对应 `基金评估系统_交付文档/11_开发规范/`） |
| 原型 | `基金评估系统原型设计说明.md` + `fund_invest_prototype.html` |
| NL 评测资产 | `06_NL选基评测/`（nl_eval_set.json ×100、nl_eval_set_adv.json ×60、nl_baseline.py、评测报告） |
| LLM 提示词 | `07_LLM提示词/nl_parse_LLM提示词设计稿.md` |

### 3.1 模块 → 详细设计 / 技术方案 对应表（实现前必须读对应行）

| 模块 | 详细设计 | 技术方案 | 原型页 |
|------|----------|----------|--------|
| 仪表盘 | §3.1 | — | ① |
| 数据中心 | §3.2 | — | ② |
| 评估引擎 | §3.3 | TP-01 | ③ |
| 智能筛选 / NL 选基 | §3.4 | TP-02 | ④ |
| 模拟交易 | §3.5 | TP-04 | ⑤ |
| 组合与配置 | §3.6 | TP-03 / TP-04 | ⑥ |
| 宏观市场认知底盘 | §3.7 | TP-05 | ⑦ |
| 单基深度实验室 | §3.8 | — | ⑧ |
| 持仓穿透 | §3.9 | — | ⑨ |
| 投教学习 | §3.10 | — | ⑩ |
| AI 助手 | §3.11 | TP-06 | ⑪ |
| 风险监控 | §3.12 | — | ⑫ |

> 跨模块：采集 §3.1.8 / §3.14；舆情 §3.9 / §3.11；调度 §3.14（分布式锁）。

---

## 4. 金融正确性红线（E1–E14 已闭环，禁止回退）

- **Brinson 归因**：用披露**真实权重** + `OTHER_CASH` 残差桶（基金/基准各匹配），**多期几何链接(Carino/Frongello)**；**严禁**将前十大归一化到权重和=1（E1/E2，TP-01 §3.3/§3.5）。
- **五因子**：`ret/risk/perf/scale/manager`；`scale`=规模健康度（非线性正向）、`manager`=任期超额（非能力，标注非能力归因）；横截面按 **`asset_class`** 分组；`ret` 权重 20（E4/E5，TP-01 §3.1）。
- **回测**：统一**后复权净值**（删 `DIVIDEND_MODE`，杜绝分红双重计）、`final_val` 扣赎回费参与 IRR；基准默认**全收益指数**、按资产类别自动选（权益→沪深300全收益 / 债券→中债综合财富 / QDII→标普500全收益）（E3/E14，TP-04）。
- **NL「稳健/低风险」**：`type ∈ [bond, mixed]`，**排除 index/etf**（E6，TP-02）。
- **组合诊断**：股债目标仓位由 `risk_type` 推导（保守 20–40% / 稳健 40–60% / 进取 60–80%）；止损=相对基准超额 < −15% **或** 回撤 > 30% 红、止盈软提示；费率预警 > 2.0%（E8/E9/E12，TP-03）。
- **宏观底盘**：ERP **拆大/小盘加权(7:3)**；估值分位固定**近 10 年窗口** + 交叉验证（偏高≠即将回调）（E10/E11，TP-05）。
- **通用**：禁用**未来函数**；`NAV=0` / 负久期 / 极值输入不崩溃；未定义口径须过 `RESEARCH_PROXY_GUARD`（错误码 40301）。

---

## 5. 架构与目录（遵循开发规范 §2）

- 模块化单体；评估引擎**唯一权威源**(ADR-002)；三层存储(ADR-003)；Redis 多副本必需(ADR-004)。
- 后端：`domain/`（算法纯逻辑，不依赖 Web）/ `api/`（FastAPI）/ `infra/`（db/redis/external/lock）/ `workers/`（采集/批算/周报）。
- **前端 Streamlit**：`app.py` + `pages/` + `components/`（Python 复合控件）+ `state.py`（`st.session_state` 封装）+ `api_client.py`（调 FastAPI）+ `utils.py`。
- 严禁 `domain` 反向依赖 `api`；配置外部化，禁止硬编码密钥。

---

## 6. 接口与错误（强制）

- 统一响应信封 `{code, data, source, as_of, disclaimer, message, trace_id}`（详细设计 §2.21 / §5.2，7 字段）；`disclaimer` 默认"仅供参考，不构成投资建议"；成功 `code=0`，失败 `data=null`。
- 错误码体系（§4.2）：`400xx` 参数/业务、`401xx` 认证、`403xx` 守卫、`429xx` 限流、`500xx` 内部、`503xx` 依赖（50301 数据源 / 50302 DB / 50303 LLM 降级）。
- SLA（§4.3）：API P95 普通 ≤ 800ms、评估/筛选 ≤ 2s；错误率 < 1%；缓存命中 ≥ 80%。
- 异常分层（开发规范 §8）：抛具体项目异常（禁裸 `Exception`），全局 handler 映射错误码；**禁止吞异常、禁止向用户泄露堆栈/SQL**。

---

## 7. 开发计划执行（开发规范 §4 + 09_开发计划）

- 任务来自 `基金评估系统_开发任务分解.md`(v1.1)；按 **Phase 0–3** 与优先级 **P0/P1/P2** 推进。
- 每任务关联 `FR-/DC-` ID；实现后核对 **DoD**：单测覆盖率 ≥ 80%、接口 100% 命中 §2.21、评审 ≥ 1、CI 全绿。
- 范围外任务不擅自做；需求/口径歧义**先提问**，不凭假设实现。

---

## 8. 质量门禁与命令（开发规范 §1.3 / §11）

- 格式化 `black`（行宽 100）+ `isort`；Lint `ruff`；类型 `mypy --strict`（新模块全开）；单函数圈复杂度 ≤ 10。
- 测试 `pytest`，覆盖率 ≥ 80%；核心算法（评分/归因/回测）须更高；接口 100% 命中 §2.21 契约。
- 提交遵循 **Conventional Commits**（`feat/fix/docs/refactor/perf/test/chore/style/ci`），关联需求 ID；PR ≥ 1 评审、CI 绿方可合并；pre-commit 钩子见 `.pre-commit-config.yaml`。

```bash
pip install -r requirements.txt
black . && ruff check . && mypy .
pytest --cov=domain --cov-report=term-missing
# 运行
uvicorn api.main:app --reload          # 内部 API
streamlit run app/app.py              # 前端
python -m workers.collect             # 采集（示例入口）
```

---

## 9. 安全与依赖（红线）

- 密钥走环境变量/密钥管理，`.env` **不入库**；日志禁止记密码/token/PII（开发规范 §5.3）。
- 外部依赖失败须**降级**（AkShare→Tushare；LLM→规则摘要 50303），不阻塞核心查询（§2.15）。
- 前端 `st.markdown(unsafe_allow_html=True)` / `st.html()` 禁止渲染未净化外部内容，确需内联 HTML 须白名单净化（bleach）。

---

## 10. 非目标 / 范围边界（MVP）

- 单用户、非多租户、非实盘交易执行（模拟/回测为主）；不接券商下单、不做高频行情。
- 不引入与现有栈冲突的依赖；新依赖须评审且有 fallback。

---

## 11. 沟通、决策与语言

- 文档/注释**中文为主**、专业术语中英对照；代码标识符英文。
- 技术选型变更须写 **ADR**（`docs/adr/`），重大变更走评审；禁止无声决策。
- 需求/口径不确定：先读文档，仍不确定则**提问**，不臆测。

---

## 12. 验证资产（实现须对齐）

- **NL 选基**：`06_NL选基评测/nl_eval_set.json`（100 条 gold）+ `nl_eval_set_adv.json`（60 条对抗），`nl_baseline.py` 为规则基线；生产解析器（LLM+规则+澄清）SLA ≥ 85%。
- 各 **TP 文档含 test oracle**（验收基准表），实现须逐条对齐；红黄绿诊断、回测曲线、归因分解均须可复算。

---

*本文件与《开发规范》《详细设计》《技术方案(TP-01~06)》《开发任务分解》共同构成编码期强制依据；条款冲突时以详细设计 + 开发规范 + 对应 TP 为准。*

# 开发期依赖（CI/pre-commit/测试，对齐开发规范§1.3 门禁 + §11 DoD）

# --- 格式与静态检查（§1.3）---
black==24.10.0
isort==5.13.2
ruff==0.8.6
mypy==1.14.1

# --- 测试 ---
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1            # TestClient transport
respx==0.22.0            # httpx mock（外部数据源 mock，就绪评估 O1）

# --- 类型 stubs（mypy --strict）---
types-redis==4.6.0.20241004
types-psycopg2==2.9.21.20250121
pydantic==2.10.4
